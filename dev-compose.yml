version: '3.1'
services:
  web:
    build: . 
    command: "bin/bundle exec rails s -p 3000 -b 0.0.0.0"
    volumes:
      - .:/app
    expose:
      - "3000"
    networks:
      - devametros
networks:
  devametros:
    driver: bridge
